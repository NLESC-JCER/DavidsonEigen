sudo: require

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - cmake
      - libblas-dev
      - liblapack-dev
      - libeigen3-dev
      
script:
  - cmake -H. -Bbuild 
  - cmake --build build
  - ./bin/test_davidson
  
branches:
  only:
   - master

